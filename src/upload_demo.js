const plupload = require('plupload');

const ACCESSID = '';
const ACCESSKEY = '';
const HOST = '';
const DIR_UPLOAD = '/';
const NOW = Date.parse(new Date()) / 1000;

let upload_name = '';

const uploader = new plupload.Uploader({

})

export default function upload_proton(btnid) {

}