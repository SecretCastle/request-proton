# Rebuild Version

Personal Tools

> stacks

* typescript

```
使用

const requestProtonInstance = new RequestProton('https://cnodejs.org/api/v1');

requestProtonInstance.req({
    url: '/topics'
}).then(res => {
    console.log(res);
})
```